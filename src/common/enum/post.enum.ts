export enum Allow_Comment_Enum {
  allow = "allow",
  notAllow = "notAllow",
}

export enum Availability_Enum {
  friends = "friends",
  only_me = "only_me",
  public = "public",
}

export enum On_Model_Enum {
  Post = "Post",
  Comment = "Comment",
}
