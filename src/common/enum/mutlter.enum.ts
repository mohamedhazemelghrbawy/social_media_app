export const multer_enum = {
  image: ["image/png", "image/jpeg", "image/webp"],
  video: ["video/mp4"],
  pdf: ["application/pdf"],
};

export enum Store_enum {
  disk = "disk",
  memory = "memory",
}
